/* eslint no-undef: "off"*/
/* eslint no-unused-expressions: "off"*/
/* eslint no-underscore-dangle: "off" */

// const app = require("../../server.js");
// const request = require("supertest")(app);
// const chai = require("chai").expect;

describe("Search", () => {
// // it("should validates that all documents, limited by a specified number and
// ordered by published date, that can be accessed by a specified role.", function (done) {

// // });

// // it("should validates that all documents, limited by a specified number,
// that were published on a certain date", function (done) {

// // });
});
